<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.valiumtavorserenase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ProgettoIntentActivity"></activity>
        <activity android:name=".TicTacToeActivity" />
        <activity android:name=".ImpostazioniActivity" />
        <activity android:name=".AppActivity" />
        <activity android:name=".GamesActivity" />
        <activity
            android:name=".games"
            android:label="@string/title_activity_games"
            android:theme="@style/NoActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdapterHome" />
    </application>

</manifest>